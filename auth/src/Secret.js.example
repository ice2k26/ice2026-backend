// The auth project no longer keeps the secret in source: the script file is
// shared read-only with all visitors (required for USER_ACCESSING web apps),
// so anything pushed here is world-readable. The secret lives in Script
// Properties under the key "SECRET" (Editor > Project Settings > Script
// Properties) and must match var SECRET in api/src/Secret.js.
// Generate with: openssl rand -hex 32
